{
  "name": "@algorandfoundation/algokit-crypto",
  "version": "0.1.0",
  "private": true,
  "description": "Interfaces and default implementations for cryptographic operations used in Algokit.",
  "author": "Algorand Foundation",
  "license": "MIT",
  "engines": {
    "node": ">=24.0"
  },
  "type": "commonjs",
  "main": "./src/index.js",
  "files": [
    "**/*"
  ],
  "scripts": {
    "test": "vitest run --coverage --passWithNoTests",
    "test:watch": "vitest watch --coverage --passWithNoTests",
    "lint": "eslint ./src/",
    "lint:fix": "eslint ./src/ --fix",
    "check-types": "tsc --noEmit",
    "audit": "better-npm-audit audit",
    "format": "prettier --config ../../.prettierrc.cjs --ignore-path ../../.prettierignore --write .",
    "pre-commit": "run-s check-types lint:fix audit format test"
  },
  "dependencies": {
    "@algorandfoundation/xhd-wallet-api": "v2.0.0-canary.1"
  }
}
