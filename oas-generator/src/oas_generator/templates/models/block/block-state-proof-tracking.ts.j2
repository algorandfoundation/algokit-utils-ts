import type { ModelMetadata } from '../core/model-runtime';
import { registerModelMeta } from '../core/model-runtime';
import type { BlockStateProofTrackingData } from './block_state_proof_tracking_data';
import { BlockStateProofTrackingDataMeta } from './block_state_proof_tracking_data';

/** Tracks state proof metadata by state proof type. */
export type BlockStateProofTracking = Record<string, BlockStateProofTrackingData>;

export const BlockStateProofTrackingMeta: ModelMetadata = {
  name: 'BlockStateProofTracking',
  kind: 'object',
  additionalProperties: { kind: 'model', meta: () => BlockStateProofTrackingDataMeta },
};

registerModelMeta('BlockStateProofTracking', BlockStateProofTrackingMeta);


