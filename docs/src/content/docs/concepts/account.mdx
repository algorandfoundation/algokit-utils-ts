---
title: 'Accounts'
description: 'Reference for the AccountManager in AlgoKit Utils (TypeScript): creating, funding, signing with, rekeying, and tracking Algorand accounts, with runnable examples and links to the full API reference.'
tableOfContents:
  minHeadingLevel: 2
  maxHeadingLevel: 3
---

import RemoteCode from '/src/components/RemoteCode.astro'
import { LinkCard } from '@astrojs/starlight/components'

Account management is one of the core capabilities provided by AlgoKit Utils. It allows you to create mnemonic, rekeyed, multisig, transaction signer, idempotent KMD and environment variable injected accounts that can be used to sign transactions as well as representing a sender address at the same time. This significantly simplifies management of transaction signing.

## Creating Accounts

In order to get/register accounts for signing operations you can use the following methods on [`AccountManager`](#accountmanager) (expressed here as `algorand.account` to denote the syntax via an [`AlgorandClient`](/algokit-utils-ts/concepts/algorand-client/)). Each returns a [`TransactionSignerAccount`](#transaction-signer-account) and registers its signer against its address, so transactions from that sender sign automatically:

| Method                                      | Returns                                                                         |
| ------------------------------------------- | ------------------------------------------------------------------------------- |
| `random()`                                  | A new, cryptographically random account                                         |
| `fromMnemonic(mnemonicSecret, sender?)`     | An account recovered from a 25-word mnemonic                                    |
| `fromEnvironment(name, fundWith?)`          | An account resolved by name — from KMD on LocalNet, or `{NAME}_MNEMONIC` beyond |
| `fromKmd(name, predicate?, sender?)`        | An account from a named KMD wallet                                              |
| `multisig(multisigParams, signingAccounts)` | A multisig account with the supplied signers loaded                             |
| `rekeyed(sender, account)`                  | An account for a rekeyed sender/signer pair                                     |
| `logicsig(program, args?)`                  | An account representing a logic signature                                       |

Each is detailed below; for full signatures and every parameter, see the [`AccountManager` API reference](/algokit-utils-ts/api/types/account-manager/classes/accountmanager/).

<LinkCard
  title="Creating an Account"
  href="https://dev.algorand.co/concepts/accounts/create"
  description="The concepts behind standalone, KMD-managed, and logic signature accounts"
/>

### Random Account Generation

Generate a new, cryptographically random account. Each account has a freshly generated private/public key pair.

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="RANDOM_ACCOUNT"
  lang="ts"
  frame="none"
/>

### Mnemonic-Based Account Recovery

Create an account from an existing 25-word mnemonic phrase, allowing account recovery and reuse of predefined test accounts. An optional second argument sets the sender address, for when the mnemonic belongs to the authorising key of a rekeyed account.

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="FROM_MNEMONIC"
  lang="ts"
  frame="none"
/>

### Environment Variable Based Account Creation

Get or create an account from environment variables. When running against LocalNet, this will create a funded wallet if it doesn't exist.

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="FROM_ENVIRONMENT"
  lang="ts"
  frame="none"
/>

On LocalNet a KMD wallet named `name` is created and funded if missing. Beyond LocalNet the account loads from `process.env['{NAME}_MNEMONIC']` (plus an optional `process.env['{NAME}_SENDER']` for rekeyed accounts), so the same code runs everywhere. Throws when `{NAME}_MNEMONIC` is missing beyond LocalNet, and requires a Node.js environment.

### KMD Account Management

When running LocalNet, you have an instance of the [Key Management Daemon](https://github.com/algorand/go-algorand/blob/master/daemon/kmd/README.md) (KMD), which you can use for:

- Accessing the private key of the default accounts that are pre-seeded with Algo so that other accounts can be funded and it's possible to use LocalNet
- Idempotently creating new accounts against a name that will stay intact while the LocalNet instance is running without you needing to store private keys anywhere (i.e. completely automated)

<a id="kmd-client-based-account-creation"></a>

The simplest way to get an account by name is `algorand.account.fromKmd`, which registers the returned account as a signer:

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="FROM_KMD"
  lang="ts"
  frame="none"
/>

Without a `predicate` it returns a random account from the wallet, and throws if no account matches.

Other operations, such as creating and renaming a wallet, can also be performed via the KMD client:

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="KMD_WALLET_ADMIN"
  lang="ts"
  frame="none"
/>

The KMD SDK is fairly low level, so to make use of it directly there is a fair bit of boilerplate. This is abstracted away into the `KmdAccountManager` class (exported from `@algorandfoundation/algokit-utils/types/kmd-account-manager`), which you can access from [`AlgorandClient`](/algokit-utils-ts/concepts/algorand-client/) via `algorand.account.kmd` or instantiate directly (passing in a `ClientManager`):

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="KMD_ACCOUNT_MANAGER"
  lang="ts"
  frame="none"
/>

The methods that are available are:

| Method                        | Description                                                 |
| ----------------------------- | ----------------------------------------------------------- |
| `getWalletAccount`            | Loads a signing account from the named KMD wallet           |
| `getOrCreateWalletAccount`    | Loads, or creates and funds, an account in the named wallet |
| `getLocalNetDispenserAccount` | Loads the default LocalNet dispenser account                |

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="KMD_MANAGER_METHODS"
  lang="ts"
  frame="none"
/>

The `predicate` is a plain function over the untyped KMD account record — `isDispenser` above is `(account) => account.status !== 'Offline' && account.amount > 1_000_000_000`. `getWalletAccount` returns `undefined` when no wallet or account matches, whereas the `algorand.account.fromKmd` equivalent above throws.

The same operations are exposed on [`AccountManager`](#accountmanager) via `algorand.account` (as with `fromKmd` above), with the added benefit of registering the returned account as a signer — including the `localNetDispenser` and `dispenserFromEnvironment` helpers shown under [Dispenser Accounts](#dispenser-accounts).

## Funding Accounts

All Algorand accounts require a minimum balance to be registered in the ledger. AlgoKit Utils provides helpers to fund accounts from a dispenser, a common need in automation and deployment scripts.

<LinkCard
  title="Funding an Account"
  href="https://dev.algorand.co/concepts/accounts/funding"
  description="Funding options across LocalNet, TestNet, and MainNet"
/>

### Dispenser Accounts

| Method                       | Description                                                      |
| ---------------------------- | ---------------------------------------------------------------- |
| `localNetDispenser()`        | The default LocalNet dispenser account                           |
| `dispenserFromEnvironment()` | A dispenser from environment variables, falling back to LocalNet |

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="DISPENSER"
  lang="ts"
  frame="none"
/>

`dispenserFromEnvironment` loads `process.env.DISPENSER_MNEMONIC` (and optionally `process.env.DISPENSER_SENDER` for a rekeyed account), falling back to the LocalNet dispenser when neither is set.

### Ensure Funded

`ensureFunded` checks the balance of an account and transfers Algo from a dispenser if the balance falls below the required spending threshold (accounting for the minimum balance requirement).

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="ENSURE_FUNDED"
  lang="ts"
  frame="none"
/>

Returns the result of the funding transaction along with the `amountFunded`, or `undefined` when the balance already met the threshold. An optional `options` argument accepts `minFundingIncrement` alongside the [common transaction and execution parameters](/algokit-utils-ts/concepts/algorand-client/#transaction-parameters).

`ensureFundedFromEnvironment` does the same, but sources the dispenser from the environment (per `dispenserFromEnvironment`), making the code portable across environments without hardcoding account details.

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="ENSURE_FUNDED_FROM_ENVIRONMENT"
  lang="ts"
  frame="none"
/>

### TestNet Dispenser API

`ensureFundedFromTestNetDispenserApi` tops up an account from the TestNet Dispenser API when its balance is below the threshold. The dispenser client is authenticated via the `ALGOKIT_DISPENSER_ACCESS_TOKEN` environment variable, so the same call works in CI/CD pipelines and automated tests.

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="TESTNET_DISPENSER_ENSURE_FUNDED"
  lang="ts"
  frame="none"
/>

It throws if the client isn't pointed at TestNet. To fund an account with a fixed amount immediately (without a balance check), use the same dispenser client directly.

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="TESTNET_DISPENSER_FUND"
  lang="ts"
  frame="none"
/>

## Keys & Signing

The `AccountManager` keeps track of which signer is associated with a given sender address. This is used by [`AlgorandClient`](/algokit-utils-ts/concepts/algorand-client/) to automatically sign transactions by that sender. Any of the [account methods](#creating-accounts) that return an account will automatically register the signer with the sender.

If you create a signer outside of the `AccountManager` — for instance when using [use-wallet](https://github.com/TxnLab/use-wallet) in a dApp — you need to register it yourself, per [Registering Signers](#registering-signers).

<LinkCard
  title="Keys and Signing"
  href="https://dev.algorand.co/concepts/accounts/keys-signing"
  description="How keys, addresses, and mnemonics are derived, and the available signing methods"
/>

### Transaction Signer Account

The core internal type that holds information about a signer/sender pair for a transaction is `TransactionSignerAccount`, which represents a `TransactionSigner` (`signer`) along with a sender address (`addr`).

`TransactionSignerAccount` can be used with the [`TransactionComposer`](/algokit-utils-ts/concepts/transactions/) and [use-wallet](https://github.com/TxnLab/use-wallet).

#### Underlying Account Classes

While `TransactionSignerAccount` is the main type used to represent an account that can sign, there are underlying account classes that can underpin the signer within the transaction signer account. You don't construct these yourself — each [account method](#creating-accounts) builds the right one and exposes it on the returned account's `account` property.

| Class             | Built by                                           | Description                                               |
| ----------------- | -------------------------------------------------- | --------------------------------------------------------- |
| `Account`         | `random()`                                         | An address and private signing key                        |
| `SigningAccount`  | `fromMnemonic()`, `fromEnvironment()`, `fromKmd()` | An account that also supports rekeying                    |
| `LogicSigAccount` | `logicsig()`                                       | A compiled logic signature and its arguments              |
| `MultisigAccount` | `multisig()`                                       | Multisig metadata plus the signing keys currently present |

`SigningAccount` and `MultisigAccount` are exported from `@algorandfoundation/algokit-utils/types/account` for the rare case where you need to build one directly.

### Default Signer

If you want to have a default signer that is used to sign transactions without a registered signer (rather than throwing an exception) then you can `setDefaultSigner`:

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="SET_DEFAULT_SIGNER"
  lang="ts"
  frame="none"
/>

### Registering Signers

There are two methods to register a signer: `setSignerFromAccount`, which takes any [account based object](#underlying-account-classes) that combines signer and sender (`TransactionSignerAccount` | `Account` | `SigningAccount` | `LogicSigAccount` | `MultisigAccount`), or `setSigner` which takes the sender address and the `TransactionSigner`. Both return the `AccountManager`, so calls can be chained:

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="REGISTER_SIGNERS"
  lang="ts"
  frame="none"
/>

`setSignerFromAccount` accepts any of the underlying account classes:

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="SET_SIGNER_FROM_ACCOUNT_TYPES"
  lang="ts"
  frame="none"
/>

### Get a Signer

[`AlgorandClient`](/algokit-utils-ts/concepts/algorand-client/) will automatically retrieve a signer when signing a transaction, but if you need to get a `TransactionSigner` externally to do something more custom then you can `getSigner` for a given sender address:

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="GET_SIGNER"
  lang="ts"
  frame="none"
/>

If there is no signer registered for that sender address it will either return the default signer ([if registered](#default-signer)) or throw an exception. `getAccount` is the equivalent that returns the whole `TransactionSignerAccount` rather than just the signer.

### Override the Signer

A signer can be overridden per transaction. Create an unsigned transaction and pass the signer explicitly when adding it to a group:

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="OVERRIDE_SIGNER"
  lang="ts"
  frame="none"
/>

## Rekeying Accounts

One of the unique features of Algorand is the ability to change the private key that can authorize transactions for an account. This is called rekeying.

<LinkCard
  title="Rekeying"
  href="https://dev.algorand.co/concepts/accounts/rekeying"
  description="How rekeying changes the authorizing key for an account"
/>

> [!WARNING]
> Rekeying should be done with caution as a rekey transaction can result in permanent loss of control of an account.

You can issue a transaction to rekey an account by using the `rekeyAccount` function. Pass the `account` to rekey and the `rekeyTo` address or signing account that will authorise it from now on; passing a signing account also registers it as the new signer. A third `options` argument accepts the [common transaction and execution parameters](/algokit-utils-ts/concepts/algorand-client/#transaction-parameters), and the result describes the rekey transaction that was sent.

You can also pass in `rekeyTo` as a [common transaction parameter](/algokit-utils-ts/concepts/algorand-client/#transaction-parameters) to any transaction.

In the following example, `accountA` is rekeyed to `accountB`. After rekeying, a transaction from `accountA` must be authorized by `accountB`'s private key.

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="REKEY_ACCOUNT"
  lang="ts"
  frame="none"
/>

> [!NOTE]
> If a signing account is passed into `algorand.account.rekeyAccount` then you don't need to call `rekeyed` to register the new signer — it is tracked automatically.

## Multisignature Accounts

A multisignature account is an ordered set of addresses with a threshold and version. The threshold determines how many signatures are required to authorize a transaction (such as 2-of-3 or 3-of-5).

<LinkCard
  title="Multisignature Accounts"
  href="https://dev.algorand.co/concepts/accounts/multisig"
  description="How multisig works at the protocol level, weighted signers, and ARC-55 coordination"
/>

Use `multisig` to register and return a multisig account with one or more signing keys loaded. The example below creates a 2-of-3 multisig account that requires only 2 signatures from the 3 possible signers to authorize transactions.

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="MULTISIG"
  lang="ts"
  frame="none"
/>

`multisig` takes the multisig metadata (its `version`, `threshold`, and member `addrs`) and the `signingAccounts` available to sign. It returns a `MultisigAccount`, whose signer collects the required signatures from those accounts automatically. Note that the address of a multisig account is derived from the metadata, so the order of `addrs` matters.

## Under the Hood

<a id="accountmanager"></a>

Throughout this page, accounts are reached through `algorand.account` — the `AccountManager` instance that an [`AlgorandClient`](/algokit-utils-ts/concepts/algorand-client/) creates and shares. `AccountManager` is the class that gets, creates, and funds accounts, and it keeps track of the signer registered for each address so that transactions sent through [`AlgorandClient`](/algokit-utils-ts/concepts/algorand-client/) or the [`TransactionComposer`](/algokit-utils-ts/concepts/transactions/) are signed by sender automatically, without a signer being specified per transaction.

To get an instance of `AccountManager`, you can use either [`AlgorandClient`](/algokit-utils-ts/concepts/algorand-client/) via `algorand.account` or instantiate it directly (passing in a [`ClientManager`](/algokit-utils-ts/capabilities/client/)):

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-ts/main/examples/concepts/accounts.algo.ts"
  snippet="ACCOUNT_MANAGER"
  lang="ts"
  frame="none"
/>

`AccountManager` is exported from `@algorandfoundation/algokit-utils/types/account-manager`.
